import { FilterByFacultyPipe } from './filter-by-faculty.pipe';

describe('FilterByFacultyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByFacultyPipe();
    expect(pipe).toBeTruthy();
  });
});
